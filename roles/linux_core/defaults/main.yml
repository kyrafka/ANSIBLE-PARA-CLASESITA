---
# linux_core/tasks/main.yml — Orquestador principal del role
# ---------------------------------------------------------------------------
# Ejecuta en orden secuencial limpiando y aplicando toda la infraestructura.
# ---------------------------------------------------------------------------

- name: "=====> Linux Core — Prerrequisitos"
  ansible.builtin.include_tasks:
    file: prerequisites.yml
  tags:
    - prerequisites
    - all

- name: "=====> Linux Core — Configuración de redes"
  ansible.builtin.include_tasks:
    file: networking.yml
  tags:
    - networking
    - netplan
    - all

- name: "=====> Linux Core — FRR y OSPF"
  ansible.builtin.include_tasks:
    file: frr.yml
  tags:
    - frr
    - routing
    - ospf
    - all

- name: "=====> Linux Core — Keepalived HA"
  ansible.builtin.include_tasks:
    file: keepalived.yml
  tags:
    - keepalived
    - ha
    - all