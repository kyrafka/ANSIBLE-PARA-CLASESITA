server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name {{ item.server_name }};
    root {{ item.root }};
    index {{ item.index }};

    location / {
        try_files $uri $uri/ =404;
    }
}
